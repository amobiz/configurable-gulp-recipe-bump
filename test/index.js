'use strict';

var Sinon = require('sinon'),
	Chai = require('chai'),
	expect = Chai.expect;

var base = process.cwd();

var bump = require(base),
	PluginError = require('gulp-util').PluginError;

describe('bump()', function () {
	it.skip('should ...', function () {
	});
});

